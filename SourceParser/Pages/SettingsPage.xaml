<Page
    x:Class="SourceParser.Pages.SettingsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:SourceParser.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid x:Name="Settings">
        <Grid.RowDefinitions>
            <RowDefinition></RowDefinition>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <StackPanel Grid.Column="0" Margin="10" Padding="0 0 5 0">
            <TextBlock Text="Настройки"/>
            <StackPanel Padding="10">
                <TextBlock Text="Импортировать обучающий список источников"/>
                <Button x:Name="ButtonImportTestSample" Content="Импортировать" Click="ButtonImportTestSample_Click" Width="auto" Height="auto" Margin="0 0 10 10"></Button>
            </StackPanel>
        </StackPanel>
    </Grid>
</Page>
